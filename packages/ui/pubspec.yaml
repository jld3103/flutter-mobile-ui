name: flutter_mobile_ui
description: A mobile UI in flutter.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  stack11_linting:
    git:
      url: https://gitlab.com/stack11-oss/linting

flutter:
  uses-material-design: true
